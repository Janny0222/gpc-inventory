import { Anek_Latin, Big_Shoulders_Display, Inter, Lusitana, Roboto, Roboto_Flex } from 'next/font/google';
import {  } from 'next/font/google';

export const inter = Inter({subsets: ['latin']});

export const lusitana = Lusitana({ subsets: ['latin'], weight: ["400", "700"]});

export const anek_latin = Anek_Latin({subsets: ['latin'], weight: ["500", "800"]})

export const roboto = Roboto_Flex({subsets: ['latin'], weight: ["400", "500", "600"]})

export const bsdisplay = Big_Shoulders_Display({subsets: ['latin'], weight: ["400", "500", "600"]})